# schemas 说明

本目录放 Skill 对外输出规范。

当前阶段建议包含：

- `research_card.schema.json`：单篇文献分析 JSON 规范。
- `research_feature_idea.schema.json`：后续调用 Skill 形成文献驱动特征想法时的输出规范。

根目录 `schemas/` 是开发时的规范源。本目录中的 schema 是 Skill 打包副本，交付前应与根目录保持一致。
